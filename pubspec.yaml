name: adaptive_material
description: A color system that means you never manually define text or icon colors again!
repository: https://github.com/caseycrogers/adaptive_material

version: 0.1.1

environment:
  sdk: '>=2.18.4 <3.0.0'
dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
